[pytest]
pythonpath = src
asyncio_mode = auto
markers =
    unit: fast isolated tests
    integration: API integration tests
    websocket: WebSocket integration tests
    stress: high-volume or stress tests
